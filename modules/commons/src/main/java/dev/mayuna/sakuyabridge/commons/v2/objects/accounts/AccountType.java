package dev.mayuna.sakuyabridge.commons.v2.objects.accounts;

/**
 * Similar to {@link dev.mayuna.sakuyabridge.commons.v2.objects.auth.AuthenticationMethods} but used as a type for accounts
 */
public enum AccountType {
    USERNAME_PASSWORD,
    DISCORD,
    ANONYMOUS;
}
